@model IEnumerable<ProyectoFinal.Models.Padecimiento>
@{
    ViewData["Title"] = "Gestionar Padecimientos";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Lista de Padecimientos</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-action="Upsert" class="btn btn-primary"><i class="bi bi-plus-circle"></i> Nuevo Padecimiento</a>
        </div>
    </div>
    <br />
    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Descripción</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Nombre</td>
                        <td>@item.Descripcion</td>
                        <td>
                            <a asp-action="Upsert" asp-route-id="@item.Id" class="btn btn-success mx-2">
                                <i class="bi bi-pencil-square"></i> Editar
                            </a>
                            <form asp-action="Delete" asp-route-id="@item.Id" method="post" style="display:inline-block">
                                <!-- Se quitó el onclick nativo y se agregó la clase btn-eliminar -->
                                <button type="button" class="btn btn-danger mx-2 btn-eliminar">
                                    <i class="bi bi-trash"></i> Eliminar
                                </button>
                            </form>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="3" class="text-center">No hay padecimientos registrados actualmente.</td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <script>

        @if (TempData["Success"] != null)
        {
                <text>
                Swal.fire({
                    title: "¡Éxito!",
                    text: "@TempData["Success"]",
                    icon: "success",
                    draggable: true
                });
                </text>
        }

        @if (TempData["Error"] != null)
        {
                <text>
                Swal.fire({
                    title: "¡Atención!",
                    text: "@TempData["Error"]",
                    icon: "warning",
                    draggable: true
                });
                </text>
        }


        document.querySelectorAll('.btn-eliminar').forEach(button => {
            button.addEventListener('click', function (e) {
                e.preventDefault();
                const form = this.closest('form');

                Swal.fire({
                    title: "¿Estás seguro?",
                    text: "¡No podrás revertir esto! El padecimiento será eliminado permanentemente.",
                    icon: "warning",
                    showCancelButton: true,
                    confirmButtonColor: "#d33",
                    cancelButtonColor: "#6c757d",
                    confirmButtonText: "Sí, eliminarlo",
                    cancelButtonText: "Cancelar",
                    draggable: true
                }).then((result) => {
                    if (result.isConfirmed) {
                        form.submit(); // Envía el formulario solo si el usuario confirma
                    }
                });
            });
        });
    </script>
}