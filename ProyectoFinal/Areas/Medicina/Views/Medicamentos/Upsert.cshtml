@model ProyectoFinal.Models.Medicamento

@{
    ViewData["Title"] = "Gestionar Medicamento";
}

<form method="post" asp-action="Upsert">
    <input type="hidden" asp-for="Id" />

    <div class="border p-3 mt-4">
        <div class="row pb-2">
            <h2 class="text-primary">@(Model.Id != 0 ? "Editar" : "Crear") Medicamento</h2>
        </div>

        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="mb-3">
            <label asp-for="Nombre"></label>
            <input asp-for="Nombre" class="form-control" />
            <span asp-validation-for="Nombre" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-primary" style="width:150px">
            @(Model.Id != 0 ? "Actualizar" : "Crear")
        </button>
        <a asp-action="Index" class="btn btn-secondary" style="width:150px">Regresar</a>
    </div>
</form>

@section Scripts {
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}